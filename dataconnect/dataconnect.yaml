specVersion: "v1beta"
serviceId: "tst"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tst-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
